import { IRepository } from './repository';
type IStockRepository = IRepository;
export default IStockRepository;
// When you add new properties in it, use the one below
// export interface IStockRepository extends IRepository{};
